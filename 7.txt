Задача регрессии. Линейная модель. Метод наименьших квадратов. Функция потерь. Метрики оценки регрессии.

Под задачей регрессии подразумевают определение математической модели (функции регрессии), устанавливающей функциональную связь между зависимой переменной и группой независимых переменных с учетом ошибки модели. Регрессия в ML - это задача ML с учителем, целью которого выступает установление взаимосвязи между различными переменными.
Модель регрессии называется линейной, если значение предсказываемого признака Y вычисляется как сумма известных признаков X1,X2,... Хm, взятых с некоторыми коэффициентами. 
Линейная комбинация признаков для прогнозирования целевого значения:
y' = w1x1 + w2x2 + ... + wmxm + w0

Парная и множественная регрессии помогают определить веса (коэффициенты) для каждой независимой переменной, которые минимизируют разницу между прогнозируемыми и наблюдаемыми значения зависимой переменной.
Метод наименьших квадратов (МНК) - метод нахождения оптимальных параметров линейной регрессии, таких, что сумма квадратов ошибок (отклонений, регрессионных остатков) минимальна. Метод заключается в минимизации расстояния между двумя векторами - вектором восстановленных значений зависимой переменной и вектором фактических значений зависимой переменной.
ну тут в файлике ищи

Функция потерь - мера количества ошибок, которые линейная регрессия делает на наборе данных. есть разные функции потерь, хотя все они вычисляют расстояние между предсказанным значением у(х) и его фактическим значением.
Метрики качества модели регрессии: коэффициент детерминации (R^2) - это доля дисперсии зависимой переменной, объясняемая рассматриваемой моделью зависимости, т.е. объясняющими переменными:
искать в файлике

MAE(средняя абсолютная ошибка) вычисляет абсолютную разницу между прогнозируемыми и фактическими значениями.
файлик

MSE(среднеквадратическая ошибка) вычисляется как сумма квадратов ошибок прогноза, деленная на кол-во точек тестовой выборки.
файлик

RMSE (корень среднеквадратичной ошибки)
МАРЕ (средняя абсолютная ошибка в процентах) 
MAE, MSE, RMSE - это функции потерь, поэтому их следует минимизировать во время обучения модели для достижения наилучшей точности предсказаний.
